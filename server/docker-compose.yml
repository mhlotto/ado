services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ado
      POSTGRES_PASSWORD: ado
      POSTGRES_DB: ado
    ports:
      - "5432:5432"
    volumes:
      - ado_pg_data:/var/lib/postgresql/data

volumes:
  ado_pg_data:
